[package]
name = "rlox"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0.18"
strum_macros = "0.28.0"

[features]
debug_trace_execution = []
debug_print_code = []

[profile.release]
opt-level = 3
strip = true
lto = "fat"
panic = "abort"
codegen-units = 1
